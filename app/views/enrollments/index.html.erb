<h1>Enrollment Results</h1>

<table>
  <% @enrollment.results.each do |r| %>
    <tr>
      <td><%= r.status %></td>
      <td><%= r.score %></td>
      <td><%= r.bookmark %></td>
      <td><%= r.created_at %></td>
    </tr>
  <% end %>
</table>
